const { Model, DataTypes } = require('sequelize');
const sequelize = require('../config/connection');

// create the Comment model
class Comment extends Model {}

// creating columns and fields for Comment model

